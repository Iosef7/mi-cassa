import PropertyNewClient from './PropertyNewClient';

export const metadata = {
  title: 'Nueva Propiedad | Mi Cassa',
  description: 'Añadir una nueva propiedad al inventario',
};

export default function NewPropertyPage() {
  return <PropertyNewClient />;
}
